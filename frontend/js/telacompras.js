/* ============================================
   NEXUS — Tela de Compras JS
   ============================================ */

(function () {
  'use strict';
  // Placeholder para integração futura com backend
  // Os cards serão gerados dinamicamente a partir dos dados recebidos

  // Exemplo de como renderizar cards vindos do backend:
  // function renderCompra(compra) {
  //   const statusClasses = {
  //     'Em Separação': 'separacao',
  //     'Em Trânsito':  'transito',
  //     'Entregue':     'entregue',
  //   };
  //   return `<article class="compra-card reveal">
  //     <div class="compra-accent compra-accent--${statusClasses[compra.status] || ''}"></div>
  //     <div class="compra-top">
  //       <span class="compra-numero">#${String(compra.id).padStart(4,'0')}</span>
  //       <span class="compra-status compra-status--${statusClasses[compra.status]}">${compra.status}</span>
  //     </div>
  //     <div class="compra-body">
  //       <div class="compra-field"><span class="field-label">Produto</span><span class="field-value">${compra.produto}</span></div>
  //       <div class="compra-field"><span class="field-label">Quantidade</span><span class="field-value">${compra.quantidade}</span></div>
  //       <div class="compra-field"><span class="field-label">Valor Total</span><span class="field-value field-value--price">R$ ${compra.valor}</span></div>
  //     </div>
  //   </article>`;
  // }

})();
